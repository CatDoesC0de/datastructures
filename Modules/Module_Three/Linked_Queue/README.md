The linked queue unlike the ring buffer queue uses dynamic allocation for getting
storage for new objects. Adding and removing elements in the linked queue are O(1) time
with the overhead of heap allocation. 
